Implementar un programa que reciba por linea de argumentos un numero entero
el programa debera crear una cola (por atras) e inserte tanto elementos como los
introducidos por linea de argumentos 

El progrma debera de Implementar la siguiente funcionalidad:
1-Mostrar lista
2-Buscar un elemento en la lista
3-Funcion que retorna la posicion de un elemento en la lista, retorna-1 si no lo encuentra
4-Funcion que calcula la media de los elementos por referencia
5-Funcion que vuelca la lista en vector 
6-Lo mismo pero con struct alumno;
struct alumno {
    char nombre [100];
    int edad;
    float nota;
}
struct nodo {
    struct alumno dato;
    struct nodo * sig;
}